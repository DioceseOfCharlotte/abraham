(function () {
  'use strict';

  var querySelector = document.querySelector.bind(document);

  var navdrawerContainer = querySelector('.navdrawer');
  var body = document.body;
  var appbarElement = querySelector('.app-bar');
  var menuBtn = querySelector('.navdrawer__toggle');
  var main = querySelector('.main-container');

  function closeMenu() {
    body.classList.remove('open');
    appbarElement.classList.remove('open');
    navdrawerContainer.classList.remove('open');
  }

  function toggleMenu() {
    body.classList.toggle('open');
    appbarElement.classList.toggle('open');
    navdrawerContainer.classList.toggle('open');
    navdrawerContainer.classList.add('opened');
  }

  main.addEventListener('click', closeMenu);
  menuBtn.addEventListener('click', toggleMenu);


      var dropMenus = document.getElementsByClassName("menu-item-has-children"),
        len = dropMenus != null ? dropMenus.length : 0,
        i = 0;
    for(i; i < len; i++){
        dropMenus[i].className += " dropdown-basic";
        dropMenus[i].dataset.dropdown += "";
    }

      var dropChild = document.getElementsByClassName("sub-menu"),
        len = dropChild != null ? dropChild.length : 0,
        i = 0;
    for(i; i < len; i++){
        dropChild[i].className += " dropdown-menu-basic";
        dropChild[i].dataset.dropdown += "";
    }



     //  	document.querySelector('.menu-item-has-children').classList.add('dropdown-basic');
    	// document.querySelector('.menu-item-has-children').dataset.dropdown = "";

    	// document.querySelector('.sub-menu').classList.add('dropdown-menu-basic');
    	// document.querySelector('.sub-menu').dataset.dropdown = "";

})();